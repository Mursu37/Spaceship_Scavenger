    To create a new computer with its own properties
    - make it it's own stateController (for example computer its "SimpleStateController.cs")
    - Make it it's own defaultState (for example computer its "MainDriveState.cs")
    - Add states that you want the computer to have in the defaultStates Contructor like in "MainDriveState.cs"
    - If you want the directories below defaultState to have their own directories just do it like it step 3
 